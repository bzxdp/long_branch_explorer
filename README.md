This is a PROTOTYPE. This was produced using ChatGPT for testing only to investigate if the idea it implements was useful. 
The test was positive and a NEW version of this software has been generated. 
The new version is written in JULIA and is in the directory called PhyJu, a growing collection of Julia code for phylogenetics. 

The new version has been written by me (mostly) but debugged using ClaudeAI. As far as I can tell the new version is not affcted by bugs.
Also as the Julia version has not been "vibe-coded" in a language I do not know (i.e. Python), I am confident the new (Julia) version does exactly what I intended this script to do. 

Finally the criteria used to identify long brahcees have been improved in the Julia version.  

The way in which long branched terminal taxa and named clades are identified as has NOT changed. 
However, how I implement the identification of local long branches using quartets and how long substrees are identified is different. 

Hence do not use this version of the code anymore download the Julia version in PhyJu instead.

This version will not be updated. 
